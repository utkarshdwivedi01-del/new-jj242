import React from "react";
import ReactDOM from "react-dom/client";
import App from "../ppl_arnold_optimized_2.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);